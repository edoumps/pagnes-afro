<% if notice %>
  <aside id="notice"><%= notice %></aside>
<% end %>

<h1>Votre Catalogue de Pagnes</h1>

<ul class="catalog">
  <% cache @products do %>
    <% @products.each do |product| %>
      <% cache product do %>
        <li>
          <%= image_tag(product.image_url_1.url) %>
          <h2><%= product.title %></h2>
          <p>
            <%= sanitize(product.description) %>
          </p>
          <div class="price">
            <%= number_to_currency(product.price) %>
            <%= button_to 'Add to Cart', order_items_path(product_id: product) %>

            <%# <%= product.price %>
          </div>
        </li>      
      <% end %>
    <% end %>
  <% end %>
</ul>
